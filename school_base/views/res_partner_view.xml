<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record model="ir.ui.view" id="view_res_partner_school_fields_form_view">
        <field name="name">view_res_partner_school_fields_form_view</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <div name="button_box" position="before">
                <field name="person_type" readonly="1" style="
                       position: absolute;
                       top: 0;
                       font-size: xx-large;
                       color: #875a7b;
                       font-weight: bold; "/>
            </div>
            <notebook position="inside">
                <page string="School data">
                    <group>
                        <group>
                            <field name="is_family"/>
                            <field name="person_type"/>
                            <field name="grade_level_id"/>
                            <field name="homeroom"/>
                            <field name="student_status"/>
                        </group>
                        <group>
                            <field name="comment_facts"/>
                            <field name="facts_id"/>
                            <field name="facts_id_int"/>
                        </group>
                    </group>
                </page>
                <page string="Financial">
                    <group>
                        <field name="financial_res_ids" widget="many2many_checkboxes" domain='["&amp;", ("id", "in", member_ids), ("person_type", "!=", "student")]'/>
                    </group>
                </page>
            </notebook>
            <xpath expr="//field[@name='child_ids']/parent::page" position="attributes">
                <attribute name="attrs">
                    {'invisible': [('is_family', '=', True)]}
                </attribute>
            </xpath>
            <xpath expr="//field[@name='parent_id']" position="attributes">
                <attribute name="attrs">
                    {'invisible': [('is_family', '=', True)]}
                </attribute>
            </xpath>
            <xpath expr="//field[@name='child_ids']/parent::page" position="before">
                <page string="Members" attrs="{'invisible': ['|', ('is_family', '=', False), ('is_company', '=', False)]}">
                    <field name="member_ids" mode="kanban" context="{'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_lang': lang, 'default_user_id': user_id}">
                        <kanban>
                            <field name="id"/>
                            <field name="color"/>
                            <field name="name"/>
                            <field name="title"/>
                            <field name="type"/>
                            <field name="person_type"/>
                            <field name="email"/>
                            <field name="parent_id"/>
                            <field name="is_company"/>
                            <field name="function"/>
                            <field name="phone"/>
                            <field name="street"/>
                            <field name="street2"/>
                            <field name="zip"/>
                            <field name="city"/>
                            <field name="country_id"/>
                            <field name="mobile"/>
                            <field name="state_id"/>
                            <field name="image_1920"/>
                            <field name="lang"/>
                            <!-- fields in form x2many view to diminish requests -->
                            <field name="comment"/>
                            <field name="display_name"/>
                            <templates>
                                <t t-name="kanban-box">
                                    <t t-set="color" t-value="kanban_color(record.color.raw_value)"/>
                                    <div t-att-class="color + (record.title.raw_value == 1 ? ' oe_kanban_color_alert' : '') + ' oe_kanban_global_click'">
                                        <div class="o_kanban_image">
                                            <t t-if="record.image_1920.raw_value">
                                                <img t-att-src="kanban_image('res.partner', 'image_1920', record.id.raw_value)"/>
                                            </t>
                                            <t t-else="">
                                                <img alt="Delivery" t-if="record.type.raw_value === 'delivery'" t-att-src="_s + &quot;/base/static/img/truck.png&quot;"/>
                                                <img alt="Invoice" t-if="record.type.raw_value === 'invoice'" t-att-src="_s + &quot;/base/static/img/money.png&quot;"/>
                                                <t t-if="record.type.raw_value !== 'invoice' || record.type.raw_value !== 'delivery'">
                                                    <img alt="Logo" t-if="record.is_company.raw_value === true" t-att-src="_s + &quot;/base/static/img/company_image.png&quot;"/>
                                                    <img alt="Avatar" t-if="record.is_company.raw_value === false" t-att-src="_s + &quot;/base/static/img/avatar_grey.png&quot;"/>
                                                </t>
                                            </t>
                                        </div>
                                        <div class="oe_kanban_details">
                                            <strong><field name="person_type"/></strong><br></br>
                                            <field name="name"/>
                                            <div t-if="record.function.raw_value"><field name="function"/></div>
                                            <div t-if="record.email.raw_value"><field name="email" widget="email"/></div>
                                            <div t-if="record.type.raw_value != 'contact'">
                                                <div>
                                                    <field name="zip"/>
                                                    <field name="city"/>
                                                </div>
                                                <field t-if="record.state_id.raw_value" name="state_id"/>
                                                <field name="country_id"/>
                                            </div>
                                            <div t-if="record.phone.raw_value">Phone: <field name="phone"/></div>
                                            <div t-if="record.mobile.raw_value">Mobile: <field name="mobile"/></div>
                                        </div>
                                    </div>
                                </t>
                            </templates>
                        </kanban>
                    </field>
                </page>
                <page string="Families" attrs="{'invisible': [('is_company', '=', True)]}">
                    <field name="family_ids" mode="kanban" context="{'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_lang': lang, 'default_user_id': user_id}">
                        <kanban>
                            <field name="id"/>
                            <field name="color"/>
                            <field name="name"/>
                            <field name="title"/>
                            <field name="type"/>
                            <field name="person_type"/>
                            <field name="email"/>
                            <field name="parent_id"/>
                            <field name="is_company"/>
                            <field name="function"/>
                            <field name="phone"/>
                            <field name="street"/>
                            <field name="street2"/>
                            <field name="zip"/>
                            <field name="city"/>
                            <field name="country_id"/>
                            <field name="mobile"/>
                            <field name="state_id"/>
                            <field name="image_1920"/>
                            <field name="lang"/>
                            <!-- fields in form x2many view to diminish requests -->
                            <field name="comment"/>
                            <field name="display_name"/>
                            <templates>
                                <t t-name="kanban-box">
                                    <t t-set="color" t-value="kanban_color(record.color.raw_value)"/>
                                    <div t-att-class="color + (record.title.raw_value == 1 ? ' oe_kanban_color_alert' : '') + ' oe_kanban_global_click'">
                                        <div class="o_kanban_image">
                                            <t t-if="record.image_1920.raw_value">
                                                <img t-att-src="kanban_image('res.partner', 'image_1920', record.id.raw_value)"/>
                                            </t>
                                            <t t-else="">
                                                <img alt="Delivery" t-if="record.type.raw_value === 'delivery'" t-att-src="_s + &quot;/base/static/img/truck.png&quot;"/>
                                                <img alt="Invoice" t-if="record.type.raw_value === 'invoice'" t-att-src="_s + &quot;/base/static/img/money.png&quot;"/>
                                                <t t-if="record.type.raw_value !== 'invoice' || record.type.raw_value !== 'delivery'">
                                                    <img alt="Logo" t-if="record.is_company.raw_value === true" t-att-src="_s + &quot;/base/static/img/company_image.png&quot;"/>
                                                    <img alt="Avatar" t-if="record.is_company.raw_value === false" t-att-src="_s + &quot;/base/static/img/avatar_grey.png&quot;"/>
                                                </t>
                                            </t>
                                        </div>
                                        <div class="oe_kanban_details">
                                            <strong><field name="person_type"/></strong><br></br>
                                            <field name="name"/>
                                            <div t-if="record.function.raw_value"><field name="function"/></div>
                                            <div t-if="record.email.raw_value"><field name="email" widget="email"/></div>
                                            <div t-if="record.type.raw_value != 'contact'">
                                                <div>
                                                    <field name="zip"/>
                                                    <field name="city"/>
                                                </div>
                                                <field t-if="record.state_id.raw_value" name="state_id"/>
                                                <field name="country_id"/>
                                            </div>
                                            <div t-if="record.phone.raw_value">Phone: <field name="phone"/></div>
                                            <div t-if="record.mobile.raw_value">Mobile: <field name="mobile"/></div>
                                        </div>
                                    </div>
                                </t>
                            </templates>
                        </kanban>
                    </field>
                </page>

                <page string="Family Invoices" readonly="0" force_save="1" attrs="{'invisible': ['|', ('is_family', '=', False), ('is_company', '=', True)]}">
                    <field name="family_invoice_ids" widget="many2many" domain="[('type', 'in', ('out_invoice', 'out_refund','out_receipt'))]">
                        <tree string="Invoices"
                            decoration-info="state == 'draft'"
                            decoration-muted="state == 'cancel'"
                            banner_route="/account/account_invoice_onboarding"
                            js_class="account_tree">
                            <field name="name"/>
                            <field name="partner_id" invisible="1"/>
                            <field name="invoice_source_email" invisible="1"/>
                            <field name="invoice_partner_icon" string=" "/>
                            <field name="invoice_partner_display_name" groups="base.group_user" string="Customer" />
                            <field name="invoice_date" optional="show" string="Invoice Date"/>
                            <field name="invoice_origin" optional="show" string="Source Document"/>
                            <field name="invoice_payment_ref" optional="hide" />
                            <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" optional="show"/>
                            <field name="invoice_date_due" optional="show"/>
                            <field name="amount_untaxed_signed" string="Tax Excluded" sum="Total" optional="show"/>
                            <field name="amount_tax_signed" string="Tax" sum="Total" optional="hide"/>
                            <field name="amount_total_signed" string="Total" sum="Total" optional="show"/>
                            <field name="amount_residual_signed" string="Amount Due" sum="Amount Due" optional="show"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="company_currency_id" invisible="1"/>
                            <field name="state" optional="show"/>
                            <field name="invoice_payment_state" optional="hide"/>
                        </tree>
                    </field>
                </page>
                <page string="Invoices">
                    <field name="invoice_ids" readonly="0" force_save="1"
                        context="{'default_type': 'out_invoice', 'default_partner_id': active_id}"
                    domain="[('type', 'in', ('out_invoice', 'out_refund','out_receipt'))]">
                        <tree string="Invoices"
                            decoration-info="state == 'draft'"
                            decoration-muted="state == 'cancel'"
                            banner_route="/account/account_invoice_onboarding"
                            js_class="account_tree">
                            <field name="name"/>
                            <field name="partner_id" invisible="1"/>
                            <field name="invoice_source_email" invisible="1"/>
                            <field name="invoice_partner_icon" string=" "/>
                            <field name="invoice_partner_display_name" groups="base.group_user" string="Customer" />
                            <field name="invoice_date" optional="show" string="Invoice Date"/>
                            <field name="invoice_origin" optional="show" string="Source Document"/>
                            <field name="invoice_payment_ref" optional="hide" />
                            <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" optional="show"/>
                            <field name="invoice_date_due" optional="show"/>
                            <field name="amount_untaxed_signed" string="Tax Excluded" sum="Total" optional="show"/>
                            <field name="amount_tax_signed" string="Tax" sum="Total" optional="hide"/>
                            <field name="amount_total_signed" string="Total" sum="Total" optional="show"/>
                            <field name="amount_residual_signed" string="Amount Due" sum="Amount Due" optional="show"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="company_currency_id" invisible="1"/>
                            <field name="state" optional="show"/>
                            <field name="invoice_payment_state" optional="hide"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

    <record id="base_view_res_partner_filter" model="ir.ui.view">
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <search position="inside">
                <field name="family_ids"/>
                <filter string="Family" name="family" domain="['&amp;', ('is_company', '=', True), ('is_family', '=', True)]"/>
                <filter string="Member" name="member" domain="['&amp;', ('is_company', '=', False), '&amp;', ('is_family', '=', False), ('family_ids', '!=', False)]"/>
                <filter string="Parent" name="parent" domain="['&amp;', ('is_company', '=', False), ('person_type', '=', 'student')]"/>
                <filter string="Student" name="student" domain="['&amp;', ('is_company', '=', False), ('person_type', '=', 'parent')]"/>
            </search>
        </field>
    </record>

    <act_window
            id="action_family"
            name="Family"
            view_mode="kanban,tree,form,activity"
            context="{'search_default_family': 1, 'default_family': True}"
            res_model="res.partner"/>
    <act_window
            id="action_member"
            name="Members"
            view_mode="kanban,tree,form,activity"
            context="{'search_default_member': 1}"
            res_model="res.partner"/>
    <act_window
            id="action_parent"
            name="Parent"
            view_mode="kanban,tree,form,activity"
            context="{'search_default_parent': 1}"
            res_model="res.partner"/>
    <act_window
            id="action_student"
            name="Student"
            view_mode="kanban,tree,form,activity"
            context="{'search_default_student': 1}"
            res_model="res.partner"/>

    <menuitem id="menu_contacts_school"
        parent="contacts.menu_contacts"
        name="School"/>

    <menuitem id="menu_contact_family"
        parent="menu_contacts_school"
        action="action_family"/>

    <menuitem id="menu_contact_member"
        parent="menu_contacts_school"
        action="action_member"/>

    <menuitem id="menu_contact_parent"
        parent="menu_contacts_school"
        action="action_parent"/>

    <menuitem id="menu_contact_student"
        parent="menu_contacts_school"
        action="action_student"/>
</odoo>